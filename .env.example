# Execucao
MONITOR_INTERVAL_SECONDS=120
LOCK_FILE_PATH=data/megahub-monitor.lock

# Navegador / Playwright
BROWSER_HEADLESS=true
PLAYWRIGHT_CHANNEL=msedge
PLAYWRIGHT_TIMEOUT_MS=30000

# Persistencia / logs
DATABASE_PATH=data/megahub-monitor.db
LOG_FILE_PATH=data/logs/monitor.log

# Configuracao declarativa
CONTEXTS_CONFIG_PATH=config/local/contexts.toml
PROFILES_CONFIG_PATH=config/local/profiles.toml

# Teams / Power Automate
# Perfis podem usar webhook_url direto no profiles.toml local
# ou apontar para variaveis de ambiente via webhook_env
TEAMS_WEBHOOK_URL=
TEAMS_REQUEST_TIMEOUT_SECONDS=15
